<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lightBlueBackground</item>
        <item name="colorPrimaryDark">@color/lightBlueBackground</item>
        <item name="colorAccent">@color/lightBlueBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
